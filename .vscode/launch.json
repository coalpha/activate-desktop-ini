{
   "version": "0.2.0",
   "configurations": [
      {
         "type": "lldb",
         "request": "launch",
         "name": "debug",
         "program": "${workspaceFolder}/bin/activate-desktop-ini.tiny.exe",
         "args": ["..\\activate-desktop-ini"],
         "cwd": "${workspaceFolder}"
      }
   ]
}
